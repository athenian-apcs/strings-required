import java.util.Scanner;

public class MyMain {

    /**
     * Checks whether a given string contains the word "the" with any capitalization.
     * @param str The string to be checked. Not null. Contains only letters and spaces, and
     *            follows grammar rules.
     * @return true if the string contains "the", false otherwise
     */
    public static boolean containsThe(String str) {
        // REPLACE WITH YOUR CODE
        return false;
    }

    /**
     * Counts the number of 'a', 'b', and 'c' chars in str.
     * @param str The string to be checked. Not null. Only contains lowercase letters.
     * @return The number of matching characters.
     */
    public static int countABC(String str) {
        // REPLACE WITH YOUR CODE
        return -1;
    }

    /**
     * Checks whether str is a palindrome or not.
     * @param str The string to be checked. Not null. Only contains lowercase letters.
     * @return true if the string is a palindrome, false otherwise.
     */
    public static boolean isPalindrome(String str) {
        // REPLACE WITH YOUR CODE
        return false;
    }

    /**
     * Takes in two Strings as input and returns a String equal to those two Strings added together
     * in alphabetic order.
     * <p>
     * Example: <code>stringOrder("banana", "apple")</code> should return <code>"applebanana"</code>.
     * @param str1 One string to be added. Might not be first alphabetically. Not null.
     * @param str2 One string to be added. Might not be second alphabetically. Not null.
     * @return The two strings added together.
     */
    public static String stringOrder(String str1, String str2) {
        // REPLACE WITH YOUR CODE
        return "";
    }

    /**
     * Takes two Strings as input, and returns a String equal to the first half
     * of the shorter String plus the second half of the longer String. The halved length
     * of the shorter String should be rounded down, but the halved length of the longer
     * String should be rounded up.
     * <p>
     * Example: <code>halfAdder("apple", "fig")</code> should return <code>"fple"</code>.
     * @param str1 One string to be half-added. Might not be shorter. Not null.
     * @param str2 One string to be half-added. Might not be longer. Not null.
     * @return The two strings smashed together.
     */
    public static String halfAdder(String str1, String str2) {
        // REPLACE WITH YOUR CODE
        return "";
    }

    public static void main(String[] args) {
    }
}
